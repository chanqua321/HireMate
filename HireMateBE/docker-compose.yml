services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: "Y"
      # Override via .env (MSSQL_SA_PASSWORD) - do not commit real secrets
      MSSQL_SA_PASSWORD: "HireMate12345!"
    ports:
      - "1433:1433"
    volumes:
      - hiremate_mssql:/var/opt/mssql
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "HireMate12345!" -C -Q "SELECT 1" || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 40s

  api:
    build: .
    ports:
      - "5080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      PORT: "8080"
      ConnectionStrings__DefaultConnection: "Server=db,1433;Database=HireMateDB;User Id=sa;Password=HireMate12345!;TrustServerCertificate=True;MultipleActiveResultSets=true"
      # Override via .env (JWT_KEY) - do not commit real secrets
      Jwt__Key: "HireMate_LocalDocker_JWT_Key_Min32Chars!"
      EmailSettings__ApiPublicUrl: "http://localhost:5080"
      EmailSettings__FrontendUrl: "http://localhost:5173"
      Cors__Origins__0: "http://localhost:5173"
      Cors__Origins__1: "http://localhost:3000"
      Swagger__Enabled: "true"
      Ai__Provider: "Heuristic"
      Ai__Enabled: "true"
      VnPay__Enabled: "false"
    depends_on:
      db:
        condition: service_healthy

volumes:
  hiremate_mssql:
